#include "monitor_instace.hpp"
int main() {
    // MonitorInstance<std::string> t(
    //     "/home/cdy1/code/project/cRaft/.data/system_data",1<<15);
    // std::thread([&]() {
    //     while (1) {
    //         t.record_batch<>({});
    //         // // t.record_ctx_swtich();
    //         // // t.record_disk();
    //         // // t.record_mem();
    //         // // t.record_network();
    //         // // t.record_thread_fd();
    //         // // t.record_cpu();
    //         // t.record_cpu();
    //     }
    // }).detach();
    // std::thread([&]() {
    //     while (1) {
    //         // t.record_ctx_swtich();
    //         // t.record_disk();
    //         // t.record_mem();
    //         // t.record_network();
    //         // t.record_cpu();
    //         // t.record_thread_fd();
    //          t.record_batch<>({});
    //     }
    // }).detach();
    // // std::thread([&]() {
    // //     while (1) {
    // //         // t.record_cpu();
    // //         // t.record_ctx_swtich();
    // //         // t.record_disk();
    // //         // t.record_mem();
    // //         // t.record_network();
    // //         // t.record_thread_fd();
    // //     }
    // // }).detach();
    // // std::thread([&]() {
    // //     while (1) {
    // //         // t.record_cpu();
    // //         // t.record_ctx_swtich();
    // //         // t.record_disk();
    // //         // t.record_mem();
    // //         // t.record_network();
    // //         // t.record_thread_fd();
    // //     }
    // // }).detach();

    // sleep(10000000);
    return 0;
}